// import { getAllContact } from "@/actions/contact/getAllContact";

const page = async () => {
  // const contact = await getAllContact();
  // if (!contact) {
  //   return <div>No contact message</div>;
  // }
  return <div>{/* <ContactPage contact={contact} /> */}</div>;
};

export default page;
